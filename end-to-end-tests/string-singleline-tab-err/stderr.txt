E0014: Unescaped tab in a single-line string

The string contains an unescaped tab. Escape the tab with `\t` or change the
string to the multi-line variant with a backquote:

  `abc	def`

The tab was found on the position below:

main.cara:1:18
━━┓
1 ┃ IO.println!("abc	def")
  ┃                 ^
