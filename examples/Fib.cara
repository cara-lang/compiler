fib(0) = 0
fib(1) = 1
fib(n) = fib(n - 1) + fib(n - 2)

IO.println(fib(20))

/*
If main is not defined in the code, the whole thing is implicitly wrapped in one big main.

Kind of the same as:

main() =

  fib(0) = 0
  fib(1) = 1
  fib(n) = fib(n - 1) + fib(n - 2)

  IO.println(fib(20))

*/
