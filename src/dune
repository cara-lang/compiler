(ocamllex (modules lexer))
(menhir 
  ;; (flags --strict)
  ;; (flags -v)
  (modules menhir_parser)
)

(executable
  (name compiler)
  (libraries
    nice_parser
    base
    core
  )
  (preprocess
    (pps 
      ppx_deriving.show
      ppx_deriving.map
      ppx_jane
    )
  )
)

